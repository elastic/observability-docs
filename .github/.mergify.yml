  - name: backport patches to 8.14 branch
    conditions:
      - merged
      - base=main
      - label=backport-8.14
    actions:
      backport:
        assignees:
          - "{{ author }}"
        labels:
          - "backport"
        branches:
          - "8.14"
        title: "[{{ destination_branch }}](backport #{{ number }}) {{ title }}"
