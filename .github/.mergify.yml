  - name: backport patches to 8.17 branch
    conditions:
      - merged
      - base=main
      - label=backport-8.17
    actions:
      backport:
        assignees:
          - "{{ author }}"
        labels:
          - "backport"
        branches:
          - "8.17"
        title: "[{{ destination_branch }}](backport #{{ number }}) {{ title }}"
