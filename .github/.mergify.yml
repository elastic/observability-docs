  - name: backport patches to 8.13 branch
    conditions:
      - merged
      - base=main
      - label=backport-8.13
    actions:
      backport:
        assignees:
          - "{{ author }}"
        labels:
          - "backport"
        branches:
          - "8.13"
        title: "[{{ destination_branch }}](backport #{{ number }}) {{ title }}"
