import GitHubIcon from './github-icon.mdx'
import Button from './button.mdx'

<div
  id="share-feedback"
  style={{
    margin: "20px 0px",
    padding: "20px",
    borderRadius: "6px",
    backgroundColor: "rgb(255, 255, 255)",
    border: "1px solid rgb(211, 218, 230)"
  }}
>
  **<GitHubIcon /> Report a problem on this page:**

  <div
    style={{
      margin: "10px 0px",
      clear: "both",
      overflow: "auto",
    }}
  >
    <Button slug={props.slug} text="Something is incorrect" labels={[ "bug", "feedback" ]} type="incorrect" />
    <Button slug={props.slug} text="Something is confusing" labels={[ "enhancement", "feedback" ]} type="confusing" />
    <Button slug={props.slug} text="Information is missing" labels={[ "enhancement", "feedback" ]} type="missing" />
    <Button slug={props.slug} text="There's a typo" labels={[ "bug", "feedback" ]} type="typo" />
  </div>
  
  **Or <a href={`https://github.com/elastic/staging-serverless-observability-docs/edit/main/${props.filepath}`}>edit this page on GitHub</a>.**
</div>