


In your `serverless.yml` file add the ((apm-lambda-ext)) Layer ARN to your function as follows:

```yml
...
functions:
  yourLambdaFunction:
    handler: ...
    layers:
      - EXTENSION_ARN
...
```
