

import ApmDataSecurityDataFilters from '../../transclusion/apm/guide/apm-data-security/data-filters.mdx'

<div id="filtering"></div>

<ApmDataSecurityDataFilters />

<div id="filters-http-header"></div>

### HTTP headers

By default, APM agents capture HTTP request and response headers (including cookies).
Most Elastic APM agents provide the ability to sanitize HTTP header fields,
including cookies and `application/x-www-form-urlencoded` data (POST form fields).
Query string and captured request bodies, like `application/json` data, are not sanitized.

The default list of sanitized fields attempts to target common field names for data relating to
passwords, credit card numbers, authorization, etc., but can be customized to fit your data.
This sensitive data never leaves the instrumented service.

This setting supports [Central configuration](((kibana-ref))/agent-configuration.html),
which means the list of sanitized fields can be updated without needing to redeploy your services:

* Go: [`ELASTIC_APM_SANITIZE_FIELD_NAMES`](((apm-go-ref-v))/configuration.html#config-sanitize-field-names)
* Java: [`sanitize_field_names`](((apm-java-ref-v))/config-core.html#config-sanitize-field-names)
* .NET: [`sanitizeFieldNames`](((apm-dotnet-ref-v))/config-core.html#config-sanitize-field-names)
* Node.js: [`sanitizeFieldNames`](((apm-node-ref-v))/configuration.html#sanitize-field-names)
    {/* * PHP: [``](https://www.elastic.co/guide/en/apm/agent/php/${variables['apm-php-branch']}) */}

* Python: [`sanitize_field_names`](((apm-py-ref-v))/configuration.html#config-sanitize-field-names)
* Ruby: [`sanitize_field_names`](((apm-ruby-ref-v))/configuration.html#config-sanitize-field-names)

Alternatively, you can completely disable the capturing of HTTP headers.
This setting also supports [Central configuration](((kibana-ref))/agent-configuration.html):

* Go: [`ELASTIC_APM_CAPTURE_HEADERS`](((apm-go-ref-v))/configuration.html#config-capture-headers)
* Java: [`capture_headers`](((apm-java-ref-v))/config-core.html#config-capture-headers)
* .NET: [`CaptureHeaders`](((apm-dotnet-ref-v))/config-http.html#config-capture-headers)
* Node.js: [`captureHeaders`](((apm-node-ref-v))/configuration.html#capture-headers)
    {/* * PHP: [``](https://www.elastic.co/guide/en/apm/agent/php/${variables['apm-php-branch']}) */}

* Python: [`capture_headers`](((apm-py-ref-v))/configuration.html#config-capture-headers)
* Ruby: [`capture_headers`](((apm-ruby-ref-v))/configuration.html#config-capture-headers)

<div id="filters-http-body"></div>

### HTTP bodies

By default, the body of HTTP requests is not recorded.
Request bodies often contain sensitive data like passwords or credit card numbers,
so use care when enabling this feature.

This setting supports [Central configuration](((kibana-ref))/agent-configuration.html),
which means the list of sanitized fields can be updated without needing to redeploy your services:

* Go: [`ELASTIC_APM_CAPTURE_BODY`](((apm-go-ref-v))/configuration.html#config-capture-body)
* Java: [`capture_body`](((apm-java-ref-v))/config-core.html#config-capture-body)
* .NET: [`CaptureBody`](((apm-dotnet-ref-v))/config-http.html#config-capture-body)
* Node.js: [`captureBody`](((apm-node-ref-v))/configuration.html#capture-body)
    {/* * PHP: [``](https://www.elastic.co/guide/en/apm/agent/php/${variables['apm-php-branch']}) */}

* Python: [`capture_body`](((apm-py-ref-v))/configuration.html#config-capture-body)
* Ruby: [`capture_body`](((apm-ruby-ref-v))/configuration.html#config-capture-body)

<div id="filters-personal-data"></div>

### Personal data

By default, the APM Server captures some personal data associated with trace events:

* `client.ip`: The client's IP address. Typically derived from the HTTP headers of incoming requests.
    `client.ip` is also used in conjunction with the [`geoip` processor](((ref))/geoip-processor.html) to assign
    geographical information to trace events.

* `user_agent`: User agent data, including the client operating system, device name, vendor, and version.

The capturing of this data can be turned off by setting
**Capture personal data** to `false`.

<div id="filters-real-user-data"></div>

### Real user monitoring data

Protecting user data is important.
For that reason, individual RUM instrumentations can be disabled in the RUM agent with the
[`disableInstrumentations`](((apm-rum-ref-v))/configuration.html#disable-instrumentations) configuration variable.
Disabled instrumentations produce no spans or transactions.

| Disable | Configuration value |
|---|---|
| HTTP requests | `fetch` and `xmlhttprequest` |
| Page load metrics including static resources | `page-load` |
| JavaScript errors on the browser | `error` |
| User click events including URLs visited, mouse clicks, and navigation events | `eventtarget` |
| Single page application route changes | `history` |

<div id="filters-database-statements"></div>

### Database statements

For SQL databases, APM agents do not capture the parameters of prepared statements.
Note that Elastic APM currently does not make an effort to strip parameters of regular statements.
Not using prepared statements makes your code vulnerable to SQL injection attacks,
so be sure to use prepared statements.

For non-SQL data stores, such as ((es)) or MongoDB,
Elastic APM captures the full statement for queries.
For inserts or updates, the full document is not stored.
To filter or obfuscate data in non-SQL database statements,
or to remove the statement entirely,
you can set up an ingest node pipeline.

<div id="filters-agent-specific"></div>

### Agent-specific options

Certain agents offer additional filtering and obfuscating options:

**Agent configuration options**

* (Node.js) Remove errors raised by the server-side process:
    disable with [captureExceptions](((apm-node-ref-v))/configuration.html#capture-exceptions).

* (Java) Remove process arguments from transactions:
    disabled by default with [`include_process_args`](((apm-java-ref-v))/config-reporter.html#config-include-process-args).

{/* **************************************************************** */}
