---
id: serverlessObservabilityApmExploreDataInElasticsearch
slug: /serverless/observability/apm-explore-data-in-elasticsearch
title: Explore data in Elasticsearch
# description: Description to be written
tags: [ 'serverless', 'observability', 'how-to' ]
status: rough content
---

import RoughContent from '../partials/rough-content-notice.mdx'

<RoughContent />

import GeneratedTransactions from '../transclusion/data/elasticsearch/generated/transactions.json'
import GeneratedSpans from '../transclusion/data/elasticsearch/generated/spans.json'
import GeneratedErrors from '../transclusion/data/elasticsearch/generated/errors.json'
import MetricsetIndicesExample from '../transclusion/metricset-indices/example.mdx'
import SourcemapBundle from '../transclusion/data/intake-api/generated/events.mdx'

<div id="exploring-es-data"></div>

* <DocLink id="serverlessObservabilityApmExploreDataInElasticsearch" section="((es))-query-examples">((es)) query examples</DocLink>

<div id="elasticsearch-query-examples"></div>

## ((es)) query examples

Elastic APM data is stored in <DocLink id="serverlessObservabilityApmDataStreams">Data streams</DocLink>.

The following examples enable you to interact with ((es))'s REST API.
One possible way to do this is using ((kib))'s
[((dev-tools-app)) console](((kibana-ref))/console-kibana.html).

Data streams, templates, and index-level operations can also be manged via ((kib))'s
[Index management](((kibana-ref))/managing-indices.html) panel.

To see an overview of existing data streams, run:

```sh
GET /_data_stream/*apm*
```
{/* CONSOLE */}

To query a specific event type, for example, application traces:

```sh
GET traces-apm*/_search
```
{/* CONSOLE */}

If you are interested in the _settings_ and _mappings_ of the Elastic APM indices,
first, run a query to find template names:

```sh
GET _cat/templates/*apm*
```
{/* CONSOLE */}

Then, retrieve the specific template you are interested in:

```sh
GET  /_template/your-template-name
```
{/* CONSOLE */}
